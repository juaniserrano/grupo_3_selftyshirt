<!DOCTYPE html>
<html lang="es">
	<!-- Include Head -->
	<%- include('../partials/head', {title: 'Perfil de usuario'}) %>
	<link rel="stylesheet" href="/css/userDetail.css" />

	<body>
		<!-- Header -->
		<%- include('../partials/header') %>
		<!-- /Header -->
		<body>
			<main>
				<div class="profile">
					<div class="profile-box">
						<div class="row">
							<div class="col-sm-8" id="profile-name">Hola, <%= user.firstName %> <%= user.lastName %> !</div>
							<div class="col-sm-4"><img src="/images/avatar/<%= user.image %>" alt="user image" class="user-img" /></div>
							<div class="col-sm-8">Su email es: <%= user.email %></div>
							<div class="col-sm-4"><button type="button" class="btn btn-success">Editar Perfil</button> <button type="button" class="btn btn-danger">ELIMINAR</button></div>
						</div>
					</div>
				</div>
			</main>
			<%- include('../partials/footer') %>
		</body>
	</body>
</html>
