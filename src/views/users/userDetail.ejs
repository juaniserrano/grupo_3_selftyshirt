<!DOCTYPE html>
<html lang="es">
	<!-- Include Head -->
	<%- include('../partials/head', {title: 'Perfil de usuario'}) %>
	<link rel="stylesheet" href="/css/userDetail.css" />

	<body>
		<!-- Header -->
		<%- include('../partials/header') %>
		<!-- /Header -->
		<main>
			<div class="profile">
				<div class="profile-box">
					<div class="row">
						<div class="col" id="profile-name">Hola, <%= user.firstName %> <%= user.lastName %> !</div>
						<div class="col"><img src="/images/avatar/<%= user.image %>" alt="user image" class="user-img" /></div>
					</div>
					<div class="row">
						<div class="col">Su email es: <%= user.email %></div>
						<div class="col">
							<button type="button" class="btn btn-success">Editar Perfil</button>
							<button type="button" class="btn btn-danger">ELIMINAR</button>
						</div>
					</div>
				</div>
			</div>
		</main>
		<%- include('../partials/footer') %>
	</body>
</html>
